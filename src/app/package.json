{
  "name": "villa-management-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "dev:ssr": "ng run villa-management-ui:serve-ssr",
    "serve:ssr": "node dist/villa-management-ui/server/main.js",
    "build:ssr": "ng build && ng run villa-management-ui:server"
  },
  "dependencies": {
    "@angular/animations": "^20.0.6",
    "@angular/common": "^20.0.6",
    "@angular/compiler": "^20.0.6",
    "@angular/core": "^20.0.6",
    "@angular/forms": "^20.0.6",
    "@angular/platform-browser": "^20.0.6",
    "@angular/platform-browser-dynamic": "^20.0.6",
    "@angular/platform-server": "^20.0.6",
    "@nguniversal/express-engine": "^20.0.0",
    "express": "^4.18.2",
    "bootstrap": "^5.3.7",
    "zone.js": "~0.15.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^20.0.4",
    "@angular/cli": "^20.0.4",
    "@angular/compiler-cli": "^20.0.6",
    "@types/express": "^4.17.21",
    "typescript": "~5.4.3"
  },
  "engines": {
    "node": "20.11.1"
  },
  "prettier": {
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  }
}
